export function setRegisterPending(state, status) {
  state.registerPending = status;
}

export function setDaysOffPending(state, status) {
  state.daysOffPending = status;
}

export function setDaysOffFetchPending(state, status) {
  state.daysOffFetchPending = status;
}

export function setDaysOffList(state, list) {
  state.daysOffList = list;
}

export function setTodaysStats(state, stats) {
  state.todaysStats = stats;
}

export function setTodaysOffs(state, offs) {
  state.todaysOffs = offs;
}

export function setTodaysStatsPending(state, status) {
  state.todaysStatsPending = status;
}

export function setApproveHRPending(state, status) {
  state.approveHRPending = status;
}

export function setApproveOperatorPending(state, status) {
  state.approveOperatorPending = status;
}

export function setApproveAdminPending(state, status) {
  state.approveAdminPending = status;
}

export function setApproveHeadPending(state, status) {
  state.approveHeadPending = status;
}
